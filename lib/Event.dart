import 'dart:ui';
class Event {
  int runTime;
  String segmentName;
  bool newSegment;
  String action;
  Color lpibgColor;

  Event (this.runTime, this.segmentName, this.newSegment, this.action, this.lpibgColor);
// Methods (Functions)
// Getters and Setters
}