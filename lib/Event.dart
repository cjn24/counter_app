import 'dart:ui';
class Event {
  int runTime;
  int eventDuration;
  String segmentName;
  String action;
  Color lpibgColor;

  Event (this.runTime, this.eventDuration, this.segmentName, this.action, this.lpibgColor);
// Methods (Functions)
// Getters and Setters
}